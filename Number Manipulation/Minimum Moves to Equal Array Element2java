class Solution {
    public int minMoves2(int[] nums) {
        Arrays.sort(nums);
        int sum=0;
        int mid=nums[nums.length/2];
        for(int num:nums){
            sum+=Math.abs(num-mid);
        }
        return sum;
    }
}